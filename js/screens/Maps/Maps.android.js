import React from 'react';
import styles from './styles';
import { Text, View } from 'react-native';
import { Font } from '../../config/styles';

const Maps = () => {
  return (
    <View style={styles.view}>
      <Text style={styles.text}>Maps is currently unavailable</Text>
    </View>
  );
};

export default Maps;
